import 'package:flutter/material.dart';

class AppColors {
  // Primary brand color (Vibe identity)
  static const Color primary = Color(0xFF6C5CE7);
  static const Color primaryDark = Color(0xFF5649C0);
  static const Color primaryLight = Color(0xFFA29BFE);

  // Backgrounds
  static const Color background = Color(0xFFF6F7FB);
  static const Color surface = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint = Color(0xFF9CA3AF);

  // Status colors
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Borders / Dividers
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFEAECEF);

  // Chat specific (we'll need later)
  static const Color messageMe = Color(0xFF6C5CE7);
  static const Color messageOther = Color(0xFFEDEDED);
}
